<article>
    <div>
        <h2><a href="/post/{{id}}">{{title}}</a></h2>
        <span class="meta">Post by {{user.username}} on {{format_date created_at}}</span>
    </div>
    <p>
        {{entry}}
    </p>
</article>